spring:
  application:
    name: marvel-message-sms
  profiles:
    active: ${maven_spring.profiles.active}
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: ${maven_nacos.ip}:${maven_nacos.port}
        group: DEV_GROUP
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: ${maven_nacos.ip}:${maven_nacos.port}